services:
  - type: web
    name: polyglot-api
    runtime: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    plan: free
    envVars:
      - key: AUTH_SECRET
        sync: false
      - key: ADMIN_EMAIL
        sync: false
